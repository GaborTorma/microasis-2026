# Neon Postgres connection string (pooled, sslmode=require).
# Get it from the Neon dashboard or `vercel env pull` after linking.
DATABASE_URL="postgresql://user:password@ep-xxx-pooler.eu-central-1.aws.neon.tech/manas?sslmode=require"
